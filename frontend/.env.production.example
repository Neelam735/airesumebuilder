# Production API base for the deployed web app. Point it at your Railway backend.
# Copy to .env.production (or set as a Vercel/Netlify env var) before building.
VITE_API_BASE=https://airesumebuilder-production-648a.up.railway.app/api/v1
